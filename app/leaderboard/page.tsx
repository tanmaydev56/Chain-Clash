'use client';
/* oxlint-disable next/no-html-link-for-pages -- Vinext RSC client navigation is unreliable in production; document navigation is the reliable fallback. */

import { useEffect, useState } from 'react';


type Entry = { player_name: string; wins: number; best_score: number };
export default function LeaderboardPage() { const [scope, setScope] = useState<'week' | 'all'>('week'); const [rows, setRows] = useState<Entry[]>([]); useEffect(() => { void fetch(`/api/game?leaderboard=1&scope=${scope}`).then((response) => response.json() as Promise<{ leaderboard?: Entry[] }>).then((data) => setRows(data.leaderboard ?? [])); }, [scope]); return <main className="min-h-dvh bg-background px-5 py-10 text-foreground"><section className="mx-auto max-w-2xl"><a href="/" className="text-sm font-bold text-primary">← Chain Clash</a><h1 className="mt-7 text-4xl font-black uppercase">Leaderboard</h1><div className="mt-5 flex gap-2">{(['week', 'all'] as const).map((item) => <button key={item} onClick={() => setScope(item)} className={`rounded-lg px-4 py-2 text-sm font-black uppercase ${scope === item ? 'bg-primary text-primary-foreground' : 'border border-white/10 text-muted-foreground'}`}>{item === 'week' ? 'This week' : 'All time'}</button>)}</div><div className="mt-5 space-y-2">{rows.map((entry, index) => <article key={`${entry.player_name}-${index}`} className="flex items-center rounded-xl border border-white/10 bg-white/[0.03] p-4"><span className="w-9 font-mono font-black text-primary">#{index + 1}</span><span className="font-bold">{entry.player_name}</span><span className="ml-auto text-sm text-muted-foreground">{entry.wins} wins · {entry.best_score} best</span></article>)}{!rows.length && <p className="rounded-xl border border-white/10 p-4 text-muted-foreground">No completed matches yet.</p>}</div></section></main>; }
